internalApiPrefix:
  /internal/api/worker/hash
springdoc:
  swagger-ui:
    path:
      /
manager:
  patchHashCrack:
    path:
      /crack/request
  baseUrl:
    http://localhost:8080/internal/api/manager/hash
server:
  port:
    8081
  error:
    include-message:
      always